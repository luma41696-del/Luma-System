# قائمة التحقق الأمني · Security Checklist

راجع هذه القائمة قبل تسليم النظام للاستخدام الفعلي، وبعد أي تعديل جوهري.

---

## 1. كلمات المرور والمصادقة

- [x] لا تُخزَّن كلمات المرور في Firestore أو Realtime Database إطلاقاً
- [x] التحقق من كلمة المرور يتم عبر Firebase Authentication حصراً
- [x] لا توجد كلمة مرور إدارية مكتوبة في HTML أو JavaScript
- [x] كلمة مرور الحساب الإداري الأول تُقرأ من متغيّر بيئة (`LUMA_ADMIN_TEMP_PASSWORD`)
- [x] الحساب الإداري الأول مُعلَّم `mustChangePassword` ويُجبر على التغيير
- [x] كلمات المرور المؤقتة تُولَّد بـ `crypto.randomInt` (CSPRNG) وتُعرض مرة واحدة
- [x] سياسة كلمة المرور: 10 أحرف + كبير + صغير + رقم + رمز — مطبّقة في الواجهة والخادم
- [x] حماية من المحاولات المتكررة: 8 محاولات → إيقاف 15 دقيقة (عدّاد على الخادم)
- [x] رد `resolveUsername` موحّد فلا يكشف وجود اسم المستخدم من عدمه
- [x] رد `requestPasswordReset` موحّد دائماً (لا يكشف الحسابات)
- [x] إعادة تعيين الصلاحيات أو تعطيل الحساب تُبطل جلسات المستخدم فوراً

## 2. التخزين المحلي في المتصفح

- [x] `localStorage` يحتوي فقط: الثيم، حالة طيّ القائمة، اسم المستخدم المحفوظ، آخر عرض مختار
- [x] لا رموز مصادقة ولا كلمات مرور ولا رواتب ولا IBAN ولا بيانات عملاء في `localStorage`
- [x] رموز Firebase تُدار من الـ SDK نفسه
- [x] «تذكّر الجلسة» = `browserLocalPersistence`، وبدونها `browserSessionPersistence`

## 3. الصلاحيات

- [x] الصلاحيات في **custom claims** يكتبها الخادم فقط
- [x] لا تُقرأ الصلاحيات من مستند يستطيع المستخدم تعديله
- [x] كل Cloud Function محمية تعيد التحقق من الصلاحية (`requireAuth` + `requirePermission`)
- [x] حماية تصعيد الصلاحيات: لا يمنح أحد صلاحية لا يملكها (`assertCanGrant`)
- [x] ترقية أي حساب إلى `admin` متاحة للمدير العام فقط
- [x] لا يمكن إزالة أو تعطيل آخر مدير نظام
- [x] لا يمكن للمستخدم تعطيل حسابه الخاص
- [x] لا يمكن لمن قدّم طلباً أن يعتمده بنفسه
- [x] القائمة الجانبية تتغيّر حسب الصلاحيات (تحسين تجربة، وليس حماية)

## 4. قواعد الأمان

- [x] Firestore: مطابقة صريحة لكل مجموعة + `match /{document=**}` يرفض كل ما عداها
- [x] الحقول الحساسة (`roles`, `perms`, `status`, `username`, `leave`) محظورة على تعديل العميل
- [x] `usernames` و`loginAttempts` و`counters` و`clientCredentials`: لا قراءة ولا كتابة من العميل
- [x] `auditLogs`: قراءة للمدير فقط، والكتابة من الخادم حصراً
- [x] الراتب والبيانات البنكية في مستندات فرعية منفصلة بصلاحيات مستقلة
- [x] المهام: المسند إليه يعدّل الحالة والتقدم فقط، لا الإسناد
- [x] الدردشة: `get()` على المحادثة الأب يمنع قراءة رسائل غرفة لست عضواً فيها
- [x] الطلبات: القارئ = مقدّم الطلب أو من يملك `requests.approve`
- [x] Storage: تحقق من نوع الملف وحجمه في كل مسار، ولا مسار عام
- [x] Realtime Database: كل مستخدم يكتب عقدة حضوره فقط، و`$other: false` يمنع الحقول الغريبة
- [x] سجلات النشاط (`activity`) للإضافة فقط — لا تعديل ولا حذف

## 5. خزنة بيانات العملاء

- [x] لا تُخزَّن أي كلمة مرور كنص صريح
- [x] AES-256-GCM مع IV عشوائي 96-bit لكل سجل ووسم مصادقة
- [x] مفتاح التشفير في **Google Secret Manager** — ليس في الواجهة ولا في قاعدة البيانات ولا في git
- [x] التشفير وفك التشفير داخل Cloud Functions فقط
- [x] النص المشفّر نفسه غير قابل للقراءة من المتصفح (قواعد Firestore تمنعه)
- [x] `vaultList` يعيد بيانات وصفية فقط — لا نص مشفّر ولا كلمات مرور
- [x] العرض يتطلب صلاحية `clients.viewCredentials` **و** إعادة مصادقة (نافذة 5 دقائق)
- [x] كلمة المرور مخفية افتراضياً في الواجهة
- [x] كل عرض/إضافة/تعديل/حذف يُسجَّل في سجل التدقيق مع المنفّذ والوقت
- [x] نافذة العرض تُغلق تلقائياً بعد 45 ثانية
- [x] الحذف يمحو المادة السرية ويُبقي أثراً للتدقيق
- [x] الواجهة توصي صراحة باستخدام Business Manager / OAuth بدل تخزين كلمة المرور
- [x] لا تُدرج بيانات الدخول ضمن أي استعلام عام للعميل

## 6. تعقيم المدخلات

- [x] `esc()` على كل قيمة تُدرج في HTML
- [x] DOMPurify على محتوى الدردشة والتعليقات
- [x] الروابط: `http/https/mailto` فقط عبر `safeUrl()` — لا `javascript:` ولا `data:`
- [x] الروابط الخارجية: `rel="noopener noreferrer nofollow"` و`target="_blank"`
- [x] إزالة محارف العرض الصفري ومحارف التحكم في الاتجاه من المدخلات
- [x] حدود طول مطبّقة في الواجهة **وفي قواعد Firestore** (العنوان 200، التعليق 4000…)
- [x] تحقق من نوع الملف وحجمه قبل الرفع وفي قواعد Storage
- [x] أسماء الملفات تُنظَّف من محارف المسار
- [x] الصور تُعاد ترميزها عبر canvas قبل الرفع (يزيل بيانات EXIF بما فيها الموقع)
- [x] تحقق من IBAN بخوارزمية mod-97 وليس بالشكل فقط

## 7. سجل التدقيق

- [x] يُسجّل: إنشاء حساب · تعطيل حساب · تغيير الأدوار · تغيير الصلاحيات · تعديل الراتب ·
      تعديل البيانات البنكية · عرض بيانات دخول · تعديلها · حذفها · قرارات الطلبات ·
      تغيير كلمة المرور · الإيقاف بعد المحاولات الفاشلة
- [x] يُخزَّن: المنفّذ، نوع الإجراء، معرّف السجل، الطابع الزمني، بيانات وصفية آمنة
- [x] عنوان الـ IP يؤخذ من سياق الطلب على الخادم فقط، لا من العميل
- [x] `scrub()` يمنع تسرّب كلمات المرور أو المفاتيح أو الـ IBAN إلى السجل
- [x] السجل غير قابل للتعديل أو الحذف من أي مستخدم

## 8. البنية التحتية

- [x] App Check (reCAPTCHA v3) مهيأ — **يجب تفعيل الإلزام (enforcement) بعد التأكد من الاستقرار**
- [x] HTTPS/TLS لكل الاتصالات (Firebase Hosting افتراضياً)
- [x] التشفير عند التخزين مفعّل (افتراضي في Firebase)
- [x] ترويسات أمنية في `firebase.json`: HSTS · nosniff · X-Frame-Options · Referrer-Policy · Permissions-Policy
- [x] `.gitignore` يستبعد `.env` وملفات حساب الخدمة والمفاتيح
- [x] الدوال في منطقة واحدة (`europe-west1`) مع `maxInstances` لتقييد التكلفة

---

## إجراءات مطلوبة من المشغّل بعد النشر

- [ ] تفعيل **App Check enforcement** لـ Firestore و Storage و Functions
- [ ] ضبط `GOOGLE_APPLICATION_CREDENTIALS` وحذف ملف حساب الخدمة بعد التهيئة
- [ ] مسح `LUMA_ADMIN_TEMP_PASSWORD` من البيئة وسجل الأوامر
- [ ] نسخة احتياطية آمنة من `VAULT_ENCRYPTION_KEY` خارج المستودع
- [ ] تفعيل النسخ الاحتياطي اليومي لـ Firestore
- [ ] تقييد النطاقات المصرّح لها في `Authentication → Settings → Authorized domains`
- [ ] ضبط تنبيهات الميزانية في Google Cloud Billing
- [ ] مراجعة سجل التدقيق دورياً (شهرياً على الأقل)
- [ ] مراجعة مصفوفة الصلاحيات بعد كل تغيير في الفريق

## نقاط تحتاج انتباهاً مستقبلياً

- **المصادقة الثنائية (2FA):** غير مفعّلة. تتطلب الترقية إلى Google Cloud Identity Platform.
- **دوال الحظر (Blocking Functions):** الإيقاف بعد المحاولات الفاشلة منفّذ عبر عدّاد
  في Firestore لأن `beforeSignIn` يتطلب Identity Platform. عند الترقية يُنقل المنطق إليها.
- **تناوب مفتاح التشفير:** لا يوجد إجراء آلي. تغيير المفتاح يتطلب إعادة إدخال بيانات الخزنة.
