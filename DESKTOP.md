# نسخة سطح المكتب (ويندوز)

برنامج ويندوز بمثبّت `.exe` — **نفس النظام تماماً**، لا نسخة مختلفة.

## كيف يعمل

```
تطبيق سطح المكتب  ┐
                  ├──▶  نفس قاعدة بيانات Firebase  ◀── نفس البيانات لحظياً
موقع المتصفح      ┘
```

الموظف على البرنامج وزميله على المتصفح **يشوفوا نفس المهام والدردشة والحضور
في نفس اللحظة**. لا يوجد فرع منفصل للواجهة: `desktop/prepare.js` يستدعي نفس
`build.js` المستخدم في نشر الموقع، فالواجهتان متطابقتان دائماً.

---

## البناء

```bash
cd desktop && npm install
```

```bash
npm run dist
```

الناتج: **`Luma-Agency-Setup-1.0.0.exe`** (حوالي 78 ميجابايت)

للتشغيل أثناء التطوير بدون بناء مثبّت:

```bash
npm start
```

وللتشغيل على المحاكي المحلي:

```bash
npm run start:emulator
```

---

## التوزيع على الموظفين

أرسل ملف `Luma-Agency-Setup-1.0.0.exe` (واتساب / فلاشة / رابط).

الموظف:
1. يفتح الملف ← **التالي** ← **تثبيت**
2. يظهر اختصار على سطح المكتب وقائمة ابدأ
3. يفتح البرنامج ويسجّل دخوله باسم المستخدم وكلمة المرور المؤقتة

> ⚠️ ويندوز قد يعرض تحذير **SmartScreen** لأن البرنامج غير موقّع رقمياً.
> الموظف يضغط **More info** ← **Run anyway**. لإزالة التحذير نهائياً تحتاج
> شهادة توقيع (Code Signing Certificate) بتكلفة سنوية — غير ضرورية لبرنامج داخلي.

---

## المزايا على المتصفح

| | |
|---|---|
| نافذة مستقلة | بدون شريط عناوين ولا تبويبات |
| أيقونة في شريط المهام | وأيقونة في منطقة التنبيهات |
| إشعارات ويندوز الأصلية | تظهر حتى والبرنامج مصغّر |
| نسخة واحدة فقط | فتح البرنامج مرتين يركّز النافذة الموجودة |
| بيانات مخزّنة محلياً | Firestore يحتفظ بنسخة، فالتصفح سريع وقت ضعف الشبكة |

---

## قرارات تقنية

**لماذا خادم محلي بدل فتح الملفات مباشرة؟**
تحت `file://` يكون الأصل (origin) مبهماً، فتتعطل مصادقة Firebase ويختلف سلوك
IndexedDB وتنهار الذاكرة المؤقتة للعمل دون اتصال. لذلك تُقدَّم الملفات من
`http://127.0.0.1` بمنفذ عشوائي — أصل حقيقي، ونفس مسارات الكود تماماً كالموقع.

**الحماية:**
- `contextIsolation: true` و `nodeIntegration: false` و `sandbox: true`
- الصفحة لا تصل إلى Node إطلاقاً — `preload.js` يكشف `isDesktop` فقط
- الخادم المحلي يرفض أي مسار خارج مجلد التطبيق
- الروابط الخارجية تُفتح في المتصفح الافتراضي، لا داخل البرنامج
- الأذونات مرفوضة افتراضياً عدا الإشعارات

---

## البناء على جهاز آخر

إذا فشل البناء برسالة `Cannot create symbolic link`:

أرشيف أدوات التوقيع يحتوي روابط رمزية لنظام ماك لا يستطيع ويندوز إنشاءها بدون
صلاحيات. الحل الأسهل: فعّل **وضع المطوّر** من

```
الإعدادات ← الخصوصية والأمان ← للمطورين ← وضع المطوّر
```

أو استخرج الأرشيف يدوياً إلى المجلد الذي يتوقعه:

```
%LOCALAPPDATA%\electron-builder\Cache\winCodeSign\winCodeSign-2.6.0
```

مع استبعاد مجلد `darwin` (خاص بماك وغير مستخدم على ويندوز).

**ملاحظة ثانية:** لا تبنِ داخل مجلد تزامنه OneDrive — يقفل الملفات أثناء الرفع
فيفشل البناء برسالة `file is being used by another process`. ابنِ خارج مجلد
المزامنة:

```bash
npx electron-builder --win --config.directories.output="%LOCALAPPDATA%\LumaAgencyBuild"
```

---

## تحديث البرنامج لاحقاً

بعد أي تعديل على الواجهة:

```bash
cd desktop && npm run dist
```

ارفع الرقم في `desktop/package.json` (`"version"`) ووزّع المثبّت الجديد —
يثبّت فوق القديم ويحتفظ بجلسة الموظف.

> لا يوجد تحديث تلقائي — يحتاج خادم تحديثات. يمكن إضافته لاحقاً عبر
> `electron-updater` مع GitHub Releases.
